# Frozen legacy compatibility inputs

`legacy/` contains immutable bytes moved from `gameskills/legacy/` at the source
commit in its provenance manifest. These are importer/rendering test inputs, not
an active instruction catalog or supported installer. Author only under
`gameskills/plugins/`. The historical README and maintainer redirects deliberately
remain unchanged; their old commands are provenance, not current instructions.
